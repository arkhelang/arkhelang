"""Emitters: thin serializers from the tool-contract IR to target formats.

Per ADR 0003, emitters carry no semantics. If an emitter needs real logic,
the IR is missing something and the fix belongs there.
"""
